# Session Handover — 2026-04-22

## Scope

- Recovered the live SEA validator from the slot-`504440` stall by deploying the validator lock-release fix already staged in `validator/src/main.rs`.
- Deployed the monitoring frontend update to Cloudflare Pages through `scripts/deploy-cloudflare-pages.sh monitoring`.
- Investigated the ghost validator shown as `V4` and the local DEX bootstrap failure.
- Reset the local chain twice, then revalidated the clean 3-validator + custody + faucet stack from scratch.

## Live-Network Findings

- The ghost validator was not SEA and not a replacement identity.
- US logs show announcements from `24.4.193.217:39129` for pubkey `8dkDiGxbzxkE7MmbrHYFHoTdEXF3V43ay2GtFtPaqSG`, `stake: 0`, `slot: 0`, `version: 0.5.6`.
- The same US logs also show:
  - repeated `Ignoring block range request from unknown peer 24.4.193.217:39129`
  - repeated reconnect attempts to `24.4.193.217:39129`
  - reconnect timeouts
- Practical conclusion: this node never reached `RegisterValidator`; it never synced past discovery. The behavior matches an external/home validator that is not actually reachable on a stable public P2P address.

## Local Bootstrap Finding

- The DEX error
  - `Missing contract addresses in symbol registry: dex_core, dex_amm, dex_router, dex_margin, dex_rewards, dex_governance, dex_analytics, prediction_market`
  was not caused by missing contracts.
- Root cause was the local RPC serving a live-synthesized signed metadata manifest with:
  - `network = lichen-testnet-1`
  - `source_rpc = live-rpc`
  while frontend local-network config expects `local-testnet`.
- Fix applied in `rpc/src/lib.rs`:
  - local dev no longer enables the live signed-metadata runtime just because `LICHEN_LOCAL_DEV=1`
  - local stacks now use the file-backed signed manifest generated by `scripts/first-boot-deploy.sh`
- Also kept the launcher persistence fix in:
  - `scripts/start-local-3validators.sh`
  - `scripts/start-local-stack.sh`
  so validators/custody/faucet survive launcher exit when started through `nohup`

## Clean Local Validation

- Clean reset path used:
  - `./scripts/stop-local-stack.sh testnet`
  - `./reset-blockchain.sh testnet`
  - `./scripts/start-local-stack.sh testnet`
- Verified after restart:
  - exactly 3 validators in `getValidators`
  - 3 nodes in `getClusterInfo`
  - signed metadata manifest reports `network = local-testnet`
  - DEX-required registry symbols resolve correctly

## Test Results

- `tests/production-e2e-gate.sh`
  - `45` stage passes
  - `0` stage failures
  - `1` intentional skip when run with `RUN_SDK_SUITE=0`
  - strict mode therefore returned non-zero because the SDK matrix was skipped on purpose to stay inside local memory limits
- Integrated gate stages that passed included:
  - RPC coverage (`117/117`)
  - live multi-validator E2E (`80/80`)
  - deep services E2E (`198/198`)
  - user services E2E (`28/28`)
  - custody withdrawal E2E (`14/14`)
  - portal interaction flows (`11/11`)
  - frontend trust boundaries (`333/333`)
  - explorer regression (`121/121`)
  - wallet flows (`70/70`)
  - developer lifecycle (`19/19`)
  - launchpad user flows (`41 pass / 0 fail / 13 expected skips`)
  - contract write scenarios (`339/339`)
  - contract deployment pipeline (`4/4`)
  - CLI comprehensive (`28/28`)
- Additional focused checks passed:
  - `bash tests/test-dex-api-comprehensive.sh` → `98 PASS / 0 FAIL / 0 SKIP`
  - `node tests/test-ws-dex.js` → `WS PASS — messages=18, acks=5, notifications=0, dex_typed_notifications=0`
  - `node tests/test_market_maker_dry_run_default.js` → `3 passed, 0 failed`

## Remaining Test Gap Observed

- `bash scripts/test-all-sdks.sh` did not pass cleanly.
- Rust SDK checks passed.
- Python SDK comprehensive check passed.
- TypeScript SDK compile passed, but the comprehensive TS run failed with:
  - `ERR_MODULE_NOT_FOUND`
  - missing module import from `sdk/js/test-all-features.ts` to `sdk/js/src/index`

## Commit / Push Status

- No commit or push was created in this session.
- The repo worktree is heavily dirty with many unrelated and pre-existing changes, so a blind mixed commit was not safe.
